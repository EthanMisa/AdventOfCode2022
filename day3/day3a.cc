#include <iostream>
#include <map>
#include <string>
#include <sstream>
using namespace std;

int main() {
    string s;
    int total = 0;
    while (!cin.eof()){

    }
    cout << "The total is: " << total << endl;
}